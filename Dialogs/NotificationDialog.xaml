<Window x:Class="Multi_Layer_Spoofing_Detector.Dialogs.NotificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification"
        Height="320"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#1E1E2E"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary"    Color="#102A43"/>
        <SolidColorBrush x:Key="TextSecondary"  Color="#2E4964"/>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground"   Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily"   Value="Segoe UI"/>
            <Setter Property="FontSize"     Value="15"/>
            <Setter Property="FontWeight"   Value="SemiBold"/>
        </Style>

        <Style x:Key="SubTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground"   Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontFamily"   Value="Segoe UI"/>
            <Setter Property="FontSize"     Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin"       Value="0,4,0,12"/>
        </Style>
    </Window.Resources>

    <Border Background="#F2F7FE" CornerRadius="12" BorderBrush="#2E2E40" BorderThickness="1" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
                <TextBlock x:Name="IconText"
                           FontSize="22"
                           Margin="0,0,10,0"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TitleText"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Style="{StaticResource TitleStyle}"
                           VerticalAlignment="Center"/>
            </DockPanel>

            <Border Grid.Row="1" Height="1" Background="#2E2E40" Margin="0,0,0,12"/>

            <TextBlock x:Name="MessageText"
                       Grid.Row="2"
                       TextWrapping="Wrap"
                       Style="{StaticResource SubTitleStyle}"
                       FontSize="14"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="SecondaryButton"
                        Width="90"
                        Height="32"
                        Margin="0,0,10,0"
                        Background="#2E2E40"
                        Foreground="#D6D6E7"
                        BorderThickness="0"
                        Visibility="Collapsed"
                        Click="SecondaryButton_Click"/>
                <Button x:Name="PrimaryButton"
                        Width="90"
                        Height="32"
                        Background="#1A2C4A"
                        Foreground="White"
                        BorderThickness="0"
                        Click="PrimaryButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
