<Window x:Class="Multi_Layer_Spoofing_Detector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI-Based Multi-Layer Spoofing Detection and Prevention System"
        Height="860" Width="1440"
        MinHeight="620" MinWidth="860"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Background="Transparent">

    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0D1325" Offset="0"/>
            <GradientStop Color="#141F3B" Offset="0.55"/>
            <GradientStop Color="#1D2C4A" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="CardBrush" Color="#1A243D"/>
        <SolidColorBrush x:Key="CardBrushSoft" Color="#223255"/>
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="SecondaryAccentBrush" Color="#06B6D4"/>
        <SolidColorBrush x:Key="SurfaceBorderBrush" Color="#34507C"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="CriticalBrush" Color="#F43F5E"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F59E0B"/>
        <SolidColorBrush x:Key="SafeBrush" Color="#10B981"/>
        <SolidColorBrush x:Key="ProtocolArpBrush" Color="#A78BFA"/>
        <SolidColorBrush x:Key="ProtocolDnsBrush" Color="#22D3EE"/>
        <SolidColorBrush x:Key="ProtocolIpBrush" Color="#60A5FA"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="RegularTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="SubTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SubTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.78"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftToggleStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="18" BorderThickness="1" BorderBrush="#38527D">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#111B32" CornerRadius="18,18,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="28" Height="28" CornerRadius="14" Background="{StaticResource PrimaryAccentBrush}" Margin="0,0,10,0">
                            <TextBlock Text="🛡" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="Multi-Layer Spoofing Detector" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock x:Name="OperationalStatusText" Text="Platform ready" Foreground="{StaticResource SubTextBrush}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="DateTimeText" Foreground="{StaticResource SubTextBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <Button x:Name="MinimizeButton" Width="38" Height="30" Click="MinimizeButton_Click" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" ToolTip="Minimize">
                            <TextBlock Text="—" FontSize="15" HorizontalAlignment="Center"/>
                        </Button>
                        <Button x:Name="MaximizeButton" Width="38" Height="30" Click="MaximizeButton_Click" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" ToolTip="Maximize">
                            <Path x:Name="MaximizeIcon" Data="M1,1 L11,1 L11,11 L1,11 Z" Stroke="White" StrokeThickness="1.2"/>
                        </Button>
                        <Button x:Name="CloseButton" Width="38" Height="30" Click="CloseButton_Click" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" ToolTip="Close">
                            <TextBlock Text="✕" FontSize="14" HorizontalAlignment="Center"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="18,18,18,12">
                    <Grid x:Name="ContentSite">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <WrapPanel Grid.Row="0" Margin="0,0,0,14" ItemWidth="300" ItemHeight="150" Orientation="Horizontal">
                            <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="Overall Network Risk" Style="{StaticResource SubTextStyle}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Border x:Name="NetworkStatusIndicator" Width="10" Height="10" CornerRadius="5" Background="{StaticResource SafeBrush}" Margin="0,6,8,0"/>
                                        <TextBlock x:Name="NetworkStatusIcon" FontSize="18" Foreground="{StaticResource TextBrush}"/>
                                        <TextBlock x:Name="NetworkStatusText" Text="SECURE" Style="{StaticResource HeaderTextStyle}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock x:Name="LastScanText" Text="Last scan: --:--:--" Style="{StaticResource SubTextStyle}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="CVSS Score" Style="{StaticResource SubTextStyle}"/>
                                    <Border x:Name="CvssBadge" Background="#1E3A5F" CornerRadius="8" Padding="10" Margin="0,10,0,0" HorizontalAlignment="Left">
                                        <StackPanel>
                                            <TextBlock x:Name="CvssScoreText" Text="0.0" FontSize="30" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock x:Name="CvssRatingText" Text="NONE" Foreground="#BFDBFE" FontSize="12" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Text="Higher scores indicate more severe impact." Style="{StaticResource SubTextStyle}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="Traffic Insights" Style="{StaticResource SubTextStyle}"/>
                                    <TextBlock x:Name="PacketsAnalyzedText" Text="0" FontSize="30" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,6,0,0"/>
                                    <TextBlock Text="Packets analyzed" Style="{StaticResource SubTextStyle}"/>
                                    <TextBlock x:Name="TotalThreatsText" Text="0 threats detected" Style="{StaticResource RegularTextStyle}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="Critical Alerts" Style="{StaticResource SubTextStyle}"/>
                                    <TextBlock x:Name="CriticalAlertsText" Text="0" FontSize="30" FontWeight="Bold" Foreground="{StaticResource CriticalBrush}" Margin="0,6,0,0"/>
                                    <TextBlock Text="Requires immediate triage" Style="{StaticResource SubTextStyle}"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>

                        <Grid Grid.Row="1" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*"/>
                                <ColumnDefinition Width="1.1*"/>
                                <ColumnDefinition Width="0.9*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,14,0">
                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="1) Data Ingestion" Style="{StaticResource HeaderTextStyle}"/>
                                        <TextBlock Text="Upload packet captures and validate files before running AI detection." Style="{StaticResource SubTextStyle}" Margin="0,6,0,12"/>
                                        <Button x:Name="UploadPcapBtn" Content="Upload .pcap / .pcapng file" Background="{StaticResource PrimaryAccentBrush}" Style="{StaticResource PrimaryButtonStyle}" Click="UploadPcapBtn_Click" Margin="0,0,0,10"/>

                                        <Border x:Name="FileInfoPanel" Background="{StaticResource CardBrushSoft}" CornerRadius="10" Padding="12" Visibility="Collapsed" Margin="0,0,0,10">
                                            <StackPanel>
                                                <TextBlock Text="Selected File" Style="{StaticResource SubTextStyle}"/>
                                                <TextBlock x:Name="FileNameText" Text="-" Style="{StaticResource RegularTextStyle}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                <Grid Margin="0,10,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Ellipse x:Name="StatusIndicator" Width="8" Height="8" Fill="{StaticResource SafeBrush}" Margin="0,5,8,0"/>
                                                    <TextBlock x:Name="FileStatusText" Grid.Column="1" Text="Ready" Style="{StaticResource SubTextStyle}"/>
                                                    <TextBlock x:Name="FileSizeText" Grid.Column="2" Text="0 MB" Style="{StaticResource SubTextStyle}"/>
                                                </Grid>
                                            </StackPanel>
                                        </Border>

                                        <Border Background="#14334F" CornerRadius="10" Padding="10">
                                            <TextBlock x:Name="UploadModuleStatus" Text="✓ Ready to accept PCAP files" Style="{StaticResource RegularTextStyle}" Foreground="{StaticResource SafeBrush}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="2) Packet Analysis" Style="{StaticResource HeaderTextStyle}"/>
                                        <TextBlock Text="Extract flow-level features and execute multi-layer spoofing inference." Style="{StaticResource SubTextStyle}" Margin="0,6,0,12"/>
                                        <Button x:Name="AnalyzeBtn" Content="Run AI Detection Pipeline" Background="{StaticResource SecondaryAccentBrush}" Style="{StaticResource PrimaryButtonStyle}" Click="AnalyzeBtn_Click" IsEnabled="False" Margin="0,0,0,8"/>
                                        <ProgressBar x:Name="AnalysisProgressBar" Height="8" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10"/>
                                        <TextBlock x:Name="AnalysisModuleStatus" Text="⏸ Waiting for PCAP file" Style="{StaticResource RegularTextStyle}" Foreground="{StaticResource WarningBrush}"/>
                                        <TextBlock x:Name="AnalysisModuleDetails" Text="Upload a file to begin." Style="{StaticResource SubTextStyle}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,14,0">
                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="3) Detection Layers" Style="{StaticResource HeaderTextStyle}"/>
                                        <TextBlock x:Name="DetectionModuleStatus" Text="Status: Idle" Style="{StaticResource SubTextStyle}" Margin="0,6,0,12"/>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Ellipse x:Name="ArpDetectionIndicator" Width="10" Height="10" Fill="{StaticResource ProtocolArpBrush}" Margin="0,4,10,0"/>
                                            <TextBlock Grid.Column="1" Text="ARP spoofing detection" Style="{StaticResource RegularTextStyle}"/>
                                        </Grid>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Ellipse x:Name="DnsDetectionIndicator" Width="10" Height="10" Fill="{StaticResource ProtocolDnsBrush}" Margin="0,4,10,0"/>
                                            <TextBlock Grid.Column="1" Text="DNS spoofing detection" Style="{StaticResource RegularTextStyle}"/>
                                        </Grid>
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Ellipse x:Name="IpDetectionIndicator" Width="10" Height="10" Fill="{StaticResource ProtocolIpBrush}" Margin="0,4,10,0"/>
                                            <TextBlock Grid.Column="1" Text="IP spoofing detection" Style="{StaticResource RegularTextStyle}"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Threat Intelligence (MITRE)" Style="{StaticResource HeaderTextStyle}"/>
                                        <ItemsControl x:Name="MitreBullets" Margin="0,10,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                        <TextBlock Text="•" Foreground="{StaticResource SecondaryAccentBrush}" Margin="0,0,6,0"/>
                                                        <TextBlock Text="{Binding}" Style="{StaticResource SubTextStyle}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <TextBlock x:Name="InsightsText" Text="Insights will appear here after analysis." Style="{StaticResource RegularTextStyle}"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Live Alerts" Style="{StaticResource HeaderTextStyle}"/>
                                        <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                                            <StackPanel x:Name="AlertsPanel"/>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Recent Cases" Style="{StaticResource HeaderTextStyle}"/>
                                        <ListBox x:Name="RecentCasesList" Background="#0F172A" BorderThickness="0" Foreground="{StaticResource TextBrush}" Margin="0,10,0,10" Height="150"/>
                                        <Button Content="Refresh recent cases" Click="RefreshRecentCasesBtn_Click" Background="#334155" Style="{StaticResource PrimaryButtonStyle}" Margin="0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="Detection Outputs" Style="{StaticResource HeaderTextStyle}"/>
                                    <TextBlock Text="Structured findings by spoofing class." Style="{StaticResource SubTextStyle}" Margin="0,6,0,12"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#172742" CornerRadius="10" Padding="10" Margin="0,0,10,0">
                                            <StackPanel>
                                                <TextBlock Text="ARP" Style="{StaticResource RegularTextStyle}" FontWeight="SemiBold"/>
                                                <StackPanel x:Name="ArpResultsPanel" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Grid.Column="1" Background="#172742" CornerRadius="10" Padding="10" Margin="0,0,10,0">
                                            <StackPanel>
                                                <TextBlock Text="DNS" Style="{StaticResource RegularTextStyle}" FontWeight="SemiBold"/>
                                                <StackPanel x:Name="DnsResultsPanel" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Grid.Column="2" Background="#172742" CornerRadius="10" Padding="10">
                                            <StackPanel>
                                                <TextBlock Text="IP" Style="{StaticResource RegularTextStyle}" FontWeight="SemiBold"/>
                                                <StackPanel x:Name="IpResultsPanel" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0">
                                <StackPanel>
                                    <TextBlock Text="Reporting &amp; Evidence" Style="{StaticResource HeaderTextStyle}"/>
                                    <TextBlock x:Name="ReportModuleStatus" Text="Ready to generate reports." Style="{StaticResource SubTextStyle}" Margin="0,6,0,10"/>
                                    <WrapPanel>
                                        <Button Content="Generate HTML report" Click="GenerateHtmlReportBtn_Click" Background="{StaticResource PrimaryAccentBrush}" Style="{StaticResource PrimaryButtonStyle}"/>
                                        <Button Content="Generate JSON report" Click="GenerateJsonReportBtn_Click" Background="#2563EB" Style="{StaticResource PrimaryButtonStyle}"/>
                                        <Button Content="Run preflight check" Click="RunPreflightCheckBtn_Click" Background="#334155" Style="{StaticResource PrimaryButtonStyle}"/>
                                        <Button Content="Export evidence bundle" Click="ExportEvidenceBundleBtn_Click" Background="#334155" Style="{StaticResource PrimaryButtonStyle}"/>
                                        <Button Content="Open reports folder" Click="OpenReportsFolderBtn_Click" Background="#334155" Style="{StaticResource PrimaryButtonStyle}"/>
                                        <Button Content="Open logs folder" Click="OpenLogsFolderBtn_Click" Background="#334155" Style="{StaticResource PrimaryButtonStyle}"/>
                                    </WrapPanel>
                                    <CheckBox x:Name="AutoRunCheckBox" Content="Auto-run analysis after valid upload" Style="{StaticResource SoftToggleStyle}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </ScrollViewer>

                <Border x:Name="LoadingOverlay" Background="#B30F172A" Visibility="Collapsed">
                    <Grid>
                        <Border Background="#1E293B" CornerRadius="14" Padding="26" Width="420" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" BorderBrush="#3B82F6">
                            <StackPanel>
                                <TextBlock Text="Processing analysis" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>
                                <ProgressBar IsIndeterminate="True" Height="10" Margin="0,14,0,10"/>
                                <TextBlock x:Name="LoadingProgressText" Text="Initializing pipeline..." Style="{StaticResource SubTextStyle}" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
